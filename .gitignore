.venv/
__pycache__/
*.egg-info/
*.pyc
# generated graph artifacts (v1: generated on-demand, not committed — DESIGN §10.9)
graph.json
*.graph.json
*.meta.json
# raw external inputs (Telegram exports, etc.) — never committed; processed into
# research/ with personal info stripped instead.
assets/
