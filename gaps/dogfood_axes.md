# codemap — реестр осей обкатки (living)

**Обновлено:** 2026-07-30
**Что это.** Единый живой реестр **осей обкатки** (dogfood axes) — под каким углом мы проверяли (и
собираемся проверять) инструмент на живом коде. Заменяет оппортунистический подход «каждая обкатка
находит следующую» на **видимое покрытие**: что закрыто, что открыто, что нашли, куда дальше.

## Как этим пользоваться (принципы)

1. **Живой, не waterfall.** Все оси заранее не перечислить — обкатка по природе discovery (ось «агент
   через serve» появилась только после M3.1; F13 не был предсказан). Новые оси **дописываем строкой**
   по мере обнаружения, а не считаем список полным.
2. **Ценность спадает.** Ранние оси находили структурное (F1 область, F5 швы); поздние — эргономику
   (F9–F13). Гнать ось стоит, пока ожидаемая находка ≥ стоимости прогона. См. «критерий остановки».
3. **Одна ось за прогон.** Каждая обкатка = один угол, предрегистрированные гипотезы, прогон на живом
   графе, findings с категорией по причине (Extraction/Representation/Query-surface/Precision/Workflow/
   Soundness), затем — реализация вехой (если гэп подтверждён и стоит того).
4. **Критерий остановки оси/программы.** Останавливаемся, когда: (а) две оси подряд дали только
   мелочь/дубли, или (б) открытые оси — только «на вырост» (scale/cross-lang/RAG-eval), или (в) появился
   живой внешний потребитель → переключаемся с «улучшать» на «пользоваться/выносить в репо-продукт».

## A. Consumer-task оси — «что карта умеет отвечать / как ей пользуются»

| # | Ось | Что проверяет | Статус | Findings → веха |
|---|-----|---------------|--------|-----------------|
| A1 | reverse-impact | «кто использует X», можно ли удалить | ✅ | F1 → M6 |
| A2 | forward call-chain | «что исполняется при X» | ✅ | F5 → M7 |
| A3 | contract/exemplar | «добавь плагин как X» (расширение) | ✅ | F4 → M9, F10 → M13 |
| A4 | string-dataflow | «проследи колонку X» | ✅ | F6 → M12, F11 → M13 |
| A5 | change-set/refactor | «что сломает смена сигнатуры» | ✅ | F7 → M11 |
| A6 | RAG-самодостаточность | «дай чанк, чтобы понять/править X» | ✅ | F3 → M10 |
| A7 | reachability/dead-code | «что мёртвое / все ли достижимо» | ✅ | F8 → M8 |
| A8 | агент-через-serve | холодный вход + сцепка шагов | ✅ | F9–F13 → M13 |
| A9 | архитектурное понимание | весь граф: слои, циклы, god-объекты, связность, хотспоты | ✅ | F18–F21 → M16 (`report architecture`) |
| **A10** | **test-mapping** | «какие тесты покрывают X / какие гонять при правке X» | ⬜ open | — |
| A11 | diff/change-review | «дан PR-дифф → blast radius, что ревьюить, что перепроверить» | ✅ | F16/F17 → M15 (`codemap review`) |
| **A12** | **onboarding/тур** | «объясни подсистему новичку», порядок чтения | ⬜ open (частично A8) | — |

## B. Trust/quality оси — «правду ли карта говорит / где ломается»

| # | Ось | Что проверяет | Статус | Findings → веха |
|---|-----|---------------|--------|-----------------|
| B1 | soundness (precision/recall) | ложные рёбра, пропущенные рёбра, вводящие в заблуждение candidate/over-set метки, неверный `canonical`-выбор | ✅ | F14/F15 → M14; мост M7 и `implements` **проверены точными** |
| **B2** | **робастность на трудном Python** | метаклассы, динамические классы, monkeypatch, `__getattr__`, условные импорты, битые файлы | ⬜ open | — |
| B3 | детерминизм/стабильность | воспроизводимость + дрейф графа на реальных правках | 🟡 частично (per-milestone тесты) | — |

## C. Non-functional оси — «на вырост»

| # | Ось | Статус / решение |
|---|-----|------------------|
| C1 | scale/performance (бóльший граф, латентность) | ⬜ отложено — networkx держит 4k узлов; связано с M3.3 SQLite |
| C2 | cross-language (не-Python) | ❌ вне v1 (DESIGN §12) |
| C3 | RAG retrieval-quality (эмбеддинги + eval) | ⬜ отложено — тяжело (нужен embed-стенд) |

## Приоритет открытых осей (моя рекомендация)

1. ~~**B1 soundness**~~ ✅ **сделано 2026-07-30** (M14). Два «страшных» приближения (мост M7,
   `implements`) — **фактически точны** (0 false-exact / 12-12 recall); два молчаливых дефекта закрыты:
   F14 (`canonical`-ambiguity → `resolved.ambiguous`), F15 (column over-set 71% шум → access-form). Схема 0.9.
2. ~~**A11 diff/change-review**~~ ✅ **сделано 2026-07-30** (M15). Вход ревьюера «дифф→что ревьюить»
   упирался в два разрыва: F16 (нет локация→символ, хотя данные есть) + F17 (нет сшивки change-set).
   Закрыты: `symbol_at`/`symbols_in_range`, serve `locate`/`review`, `codemap review <diff>` с risk-рангом.
   Риск-ранжирование сложилось бесплатно; added/deleted — граница (двух-графовый diff, отложено).
3. ~~**A9 архитектурное понимание**~~ ✅ **сделано 2026-07-30** (M16). Новый класс ценности (система
   целиком): F18 слои+нарушения (order-free), F19 coupling Ca/Ce/I, F20 god-объекты/хотспоты (pervasive-
   разметка), F21 синтез `report architecture`. Реальная находка на bquant: слоевое нарушение
   `analysis↔indicators`. Без схемы.
4. **A10 test-mapping** — практично, среднее; impact/review уже видят тесты, не хватает «символ→тесты» среза.
5. **B2 робастность** — ценно, но на bquant «трудного Python» может быть мало → риск синтетики.
6. **A12 onboarding** — пересекается с discovery (A8); ниже.
7. C1/C3 — по появлению масштаба/нужды; C2 — вне v1.

**Стоп-критерий — ДОСТИГНУТ (развилка сейчас).** Пройдены **три** приоритетные оси (B1, A11, A9) — все
дали реальные закрытые находки (F14–F21), не мелочь. Все три оси нового класса ценности — soundness,
рабочий вход ревьюера, форма системы — **закрыты**. Открытые A10/A12/B2 — уже **добор ради полноты**
(средняя/узкая ценность, риск синтетики на bquant). По правилу «ценность спадает + практические оси
закрыты» правильный следующий шаг — **не A10, а переключение на пользоваться/выносить**: тонкий
**MCP-адаптер** (serve уже transport-neutral, 21 op) и/или **вынос codemap в отдельный репо-продукт**
(P1→P2 в `devref/architecture/package_roadmap`). Обкатку осей возобновлять по **появлению нужды** (живой
потребитель нашёл новый угол), а не проактивно.

---

*Живой документ. Каждая новая обкатка: (1) добавь/отметь строку оси, (2) заведи `gaps/<axis>_<date>.md`
с планом+findings, (3) при подтверждённом гэпе — веха + отметка. Новые оси — дописывать, список не финален.*
