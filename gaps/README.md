# codemap — гэпы и аналитика покрытия

**Последнее обновление:** 2026-09-02

Документы этого раздела фиксируют **ограничения, расхождения дизайн↔реализация и пробелы**
инструмента `codemap/` — статического анализатора кода, который строит queryable граф пакета.

Раздел живёт рядом с `DESIGN.md` и `BACKLOG.md` в корне репо. codemap выделен в самостоятельный
репо-продукт из монорепо `bquant`, где инкубировался; `bquant` остаётся подопытной мишенью обкатки
(`codemap build ../bquant` — как сторонний тул на внешнем пакете).

## Реестр осей обкатки

**[dogfood_axes.md](dogfood_axes.md)** — единый **живой реестр осей** (под каким углом проверяем
инструмент): что закрыто (A1–A8 → findings F1–F13), что открыто (A9–A12 задачи, B1 soundness, B2
робастность), приоритет и стоп-критерий. Начинать новую обкатку — отсюда.

## Документы

| Документ | Назначение |
|----------|------------|
| **[coverage_gap_analysis_2026-07-24.md](coverage_gap_analysis_2026-07-24.md)** | Исчерпывающий разбор: насколько полно codemap покрывает код `bquant`, какие связи между сущностями есть и отсутствуют, понимание моделей данных, потоков данных и прочих семантических характеристик кода. Эмпирика: прогон `codemap build ../bquant`. Статусы CM-01…CM-14 обновляются по мере закрытия (M1.5, M4). |
| **[call_resolution_spike_2026-07-26.md](call_resolution_spike_2026-07-26.md)** | Измерительный спайк + решение (тэйк): потолок разрешения вызовов по именам (~19%) vs jedi-вывод типов (~28%); зачем берём jedi, два тира (fast/deep), граница v1. Основа вехи M5. |
| **[observability_dogfood_2026-07-28.md](observability_dogfood_2026-07-28.md)** | Обкатка видов M2 (RAG/vault/mermaid) на живых вопросах о bquant. Findings F1…F4; главный (F1) — слепота по области (индексировали только пакет). Основа вехи M6 (repo scope). §6 — обкатка-2: **F1/F2 закрыты** (`report impact` даёт полный blast radius, tests 19 совпало с grep). Вердикт: `MACDZoneAnalyzer` безболезненно не выпилить. F3/F4 открыты. |
| **[dispatch_bridging_2026-07-28.md](dispatch_bridging_2026-07-28.md)** | Навигационная обкатка «что при `analyze_zones`». Гэп **F5** — слепота на швах диспетчеризации (фабрика+реестр+вызов на объекте): цепочка рвётся там, где живёт реальная работа плагинов. Решение — веха M7 (registry-aware bridging по таблице M1.5). Результат: цепочка 38→**116** узлов, швы реконнекчены. **F5 закрыт.** |
| **[deep_dogfood_2026-07-29.md](deep_dogfood_2026-07-29.md)** | Глубокая обкатка на **5 непокрытых осях** (contract/exemplar, dataflow по строковому ключу, change-set reasoning, RAG-самодостаточность, reachability), гипотезы H1…H5 до прогона. Нашла 3 новых гэпа (**F6** dataflow строк.ключ, **F7** арг-контракт call-site, **F8** провенанс dead-code) + подтвердила **F3**/**F4**; M7×dead-code честно опровергнута. **Все 5 закрыты в тот же день вехами M8–M12** (схема 0.5→0.8, тесты 57→83): F8 провенанс dead-code, F4 `implements`-семейства, F3 class-chunk агрегация, F7 арг-контракт, F6 `column`-узлы/`reads`/`writes`. |
| **[agent_workflow_dogfood_2026-07-29.md](agent_workflow_dogfood_2026-07-29.md)** | Обкатка на **новой оси** — рабочий цикл ИИ-агента через тёплый `serve` (M3.1): где рвётся *сцепка шагов*, а не один ответ. **Итог: 5 новых гэпов (Workflow/Query-surface).** **F9** нет discovery/поиска/обзора (холодный старт невозможен). **F10** extension-рецепт неполон (нет декоратора регистрации в ответе). **F11** dataflow только по ключу, нет «колонки функции F». **F12** query-ответ без `file:line` + нет `source`-op (агент не дойдёт до кода). **F13** реляционные ops не резолвят re-export → цепочка `query→implementers` молча пуста. **Все 5 закрыты вехой M13** (2026-07-29): новые ops `search`/`families`/`columns_of`/`source`/`resolve`, канонизация имён, `file:line`+`registered_as`+`columns` в query-ответе; serve 13→18 ops, тесты 91→98. |
| **[soundness_dogfood_2026-07-30.md](soundness_dogfood_2026-07-30.md)** | Обкатка **оси B1 (trust)** — precision/recall трёх «честных приближений» + `canonical`, сверкой с исходником bquant. **Итог: два приближения фактически точны** (мост M7 — 0 false-exact, веер честен; `implements` — 12/12 истинны, recall 100%); **два реальных, молчаливых дефекта доверия**: **F14** `canonical` молча выбирает 1 из N (до 25) без сигнала → реляционные ops уверенно врут про не тот символ (вплоть до тест-мока); **F15** 71% `column`-узлов — dict-literal payload-ключи, не колонки → агрегат `columns()` вводит в заблуждение. **Оба закрыты вехой M14** (2026-07-30, схема 0.9): `canonical_info`+`resolved.ambiguous` в конверте; access-form на column-узле/рёбрах, `columns()` отдаёт реальный ~30%; тесты 98→103. |
| **[changereview_dogfood_2026-07-30.md](changereview_dogfood_2026-07-30.md)** | Обкатка **оси A11 (diff/change-review)** — вход ревьюера «дан PR-дифф → что ревьюить», ведён строго через `serve` на реальном диффе bquant. **Итог: два разрыва во входе ревьюера.** **F16** нет резолвера локация→символ (данные containment `file`+`lineno`+`endlineno` есть, поверхности нет — `search` только по подстроке id). **F17** нет сшивки change-set: 4 символа = ~20 ручных вызовов, риск не синтезирован. **Оба закрыты вехой M15** (2026-07-30, **без схемы**): `Query.symbol_at`/`symbols_in_range` (fallback к модулю), serve `locate`/`review`, `codemap review <diff>` (парсер unified-хунков) → risk-ранжированное досье; serve 18→20 ops, тесты 103→116. Проверено на `git diff` (7 хунков→10 символов, high-risk первыми). Границы: added/deleted → двух-графовый diff (отложено). |
| **[architecture_dogfood_2026-07-30.md](architecture_dogfood_2026-07-30.md)** | Обкатка **оси A9 (архитектурное понимание)** — роль архитектора «форма системы целиком», при том что было только `report dependencies` (циклы+top-imported). **Итог: 4 гэпа глобального вида.** **F18** нет слоёв+направления+нарушений; **F19** нет coupling Ca/Ce/Instability; **F20** нет god-объектов/хотспотов; **F21** нет синтез-отчёта. **Все закрыты вехой M16** (2026-07-30, **без схемы**): `Query.layers()` (violations order-free), `coupling()`, `hotspots()` (pervasive-разметка логгер-шума), `serve/architecture.py` + `report architecture` + op `architecture`; report-kinds 4→5, ops 20→21, тесты 116→123. Реальная находка на bquant: слоевое нарушение `analysis↔indicators`. **Это последняя ось нового класса ценности — стоп-критерий достигнут, развилка на «пользоваться/выносить».** |
| **[attribute_impact_gap_2026-08-22.md](attribute_impact_gap_2026-08-22.md)** | Гэп чести из dogfood при планировании rename поля dataclass: `impact` на **атрибуте класса** возвращал `refs:[]` и **`risk:"none"`** — атрибут-ноды извлекались, но ни одно ребро в них не входило. Не «нет данных», а **отсутствие различия**: аффирмативное «ничего не зависит» там, где честный ответ `unknown`. Закрыт вехой **R1-C20** (2026-08-23, схема 0.11): edge-type `accesses` + пасс `attrflow.py` + `risk:"unknown"` вместо `none`. Issue [#1](https://github.com/kogriv/codemap/issues/1). |
| **[flat_layout_gap_2026-08-24.md](flat_layout_gap_2026-08-24.md)** | Ось **B2 (робастность)** на **втором реальном таргете** (плоский каталог модулей, не пакет) — оба дефекта в первые 20 минут первой сборки. Две половины одного layout'а: **(a)** без `__init__.py` — краш на namespace-пакете (`filepath` = `list`), сообщение не называет причину; **(b)** с `__init__.py` — сборка **успешна и молча даёт 0 `imports`-рёбер**, отчёты рендерят отсутствие данных как **справку о здоровье** («no layer violations», «ацикличен»), а `dead-code` объявляет весь живой движок orphan. Тот же класс, что F14/#3 — уверенное **ничто**. Замерено: правило sibling-резолва даёт 0 ложных на codemap/bquant; краш живёт в 5 местах. **Закрыт в тот же день вехой R1-C21** (2026-08-24, без схемы): резолв sibling-импортов с пометкой `extras.resolution="flat"`, нормализация `filepath` в одной точке (краш жил в 5 потребителях), дериватив-диагностика «0 imports при ≥2 модулях» на билде / в `stats` / в трёх отчётах; граф bquant **байт-в-байт идентичен**, тесты 369→389. Дизайн `../docs/design/flat_layout.md` (D1–D5 закрыты: A/A/да/нормализация+продолжать/derive; §10 — промах дизайна: слой вызовов был слеп так же). Issues [#4](https://github.com/kogriv/codemap/issues/4), [#5](https://github.com/kogriv/codemap/issues/5). |
| **[dead_code_high_band_2026-08-24.md](dead_code_high_band_2026-08-24.md)** | Гэп чести в **действенной** полосе: `high` («нет входящих ссылок») выставляется функции, на которую ссылка есть в её же модуле. Замерено на двух пакетах — **три** механизма, не один: ① функция-как-значение (codemap 13), ② вызов на уровне модуля (bquant 2), ③ вызов из вложенного def (codemap 4 / bquant 1); **20 из 51 (39%) ложных**, причём каждый пакет показывает лишь два из трёх. ② и ③ — отсутствующие `calls`-рёбра, значит страдает и `impact`/`flows`. **Закрыт в тот же день вехой R1-C22** (2026-08-24, без схемы): все три формы смоделированы (`references` name/annotation, `calls` от модуля, атрибуция вложенных с `via="nested"`); `high` 46→29 / 5→2, только добавления (bquant +364, 0 удалений), тесты 401→415. Попутно исправлен устаревший эталон бенчмарка `c10_closure`. Дизайн `../docs/design/source_visible_references.md` (D1–D5 закрыты). Issue [#7](https://github.com/kogriv/codemap/issues/7). |
| **[hard_python_robustness_2026-08-25.md](hard_python_robustness_2026-08-25.md)** | Ось **B2 (робастность)**, замер **упреждающий** — не репорт извне. Проба на 17 файлов, по модулю на конструкцию. Сборка exit 0 и **ни одного предупреждения**. Держится большинство «трудного Python» (метаклассы, `type()`-класс — записан как attribute, а не выдуман; PEP 562/695, `match`, async, `singledispatch`, monkeypatch), **`dead-code high` пуст**. Но **пять молчаливых дыр**: ① симлинк-цикл → **615 модулей вместо 15** (2378 узлов вместо 58), при том что `codemap scope` на том же дереве говорит `files: 17` — две половины тула расходятся в 36×; ② нечитаемый файл (синтаксис / не-UTF-8) **исчезает без слова**; ③ `from X import *` не даёт `imports`-ребра; ④ строковые/`TYPE_CHECKING`-аннотации невидимы; ⑤ stub-only `.pyi` подаётся как настоящий код. **Закрыт в тот же день** вехой **R1-C23** (2026-08-25, без своей схемы — едет на 0.12 от R1-C25): симлинк даёт **15 модулей вместо 615**; нечитаемые файлы **названы** и едут в `provenance.inputs.skipped` с причиной; `imports` для star-импорта; квотированные аннотации резолвятся; stub помечен и исключён из dead-code; **D6 — закон сохранения «модули vs входные файлы»** в обе стороны. Дельта замерена **на замороженных копиях между двумя версиями тула**: bquant **+26 рёбер, 0 удалений** (все — аннотации), codemap **+3, 0**; узлы не изменились. Тесты 453→**484**. **Ловушка замера:** первое «до/после» соврало, потому что griffe разрешил имя пакета из `sys.path` (cwd), а не из `search_paths` — старый тул читал собственный исходник; заодно это дало правку в R1-C25 (`tool.dirty`). Дизайн `../docs/design/hard_python_robustness.md` (D1–D7 закрыты), доки `../docs/hard-python.md`. |
| **[test_mapping_2026-08-25.md](test_mapping_2026-08-25.md)** | Ось **A10 (test-mapping)** — «какие тесты гонять при правке X». Op'а нет; ближайшие `callers`/`impact` дают **прямое** ребро, которого нет у **82%** символов (68/380). Достижимость от тестов: **59% fast / 80% deep**. Причина разрыва — **методы**: входящие `calls` разрешены у 91% свободных функций и лишь у **21% методов** (deep 56%), а тесты зовут методы на объекте. Достижимый ответ **слишком велик**: медиана 21 тест, максимум 126 из 416; ближняя полоса — медиана 6.5, p90 66 → **ранжирование это и есть фича**. Шов pytest-фикстур не смоделирован (63% тестов получают предмет параметром, **0** рёбер), но замеренная цена — 0 символов, т.е. вопрос **атрибуции**; свидетель слабый — у codemap нет `conftest.py`. **Закрыт в тот же день** вехой **R1-C24** (2026-08-25, без схемы): `Query.tests_for`/`covers`, ops `tests`/`covers`, CLI `codemap tests` с готовой pytest-командой. **Приёмка против `coverage.py`, и замер решил два вопроса за дизайн:** ① отсечка по дистанции — **обрыв, а не спад** (медианная точность 1.00 / 1.00 / 1.00 на 1–3 хопах и **0.67 при 78 тестах** на четвёртом), дефолт 3 взят из таблицы; ② **«recall ближней полосы» оказался неверной метрикой приёмки, хотя дизайн называл её главной** — против истины coverage медиана recall — **0.43** по отвеченным символам и **0.02** по всем покрытым (unknown = ноль), и проваливается она ровно там, где истинное множество шире всего: у `Graph.add_edge` оно **151 тест**, то есть весь сьют; значимым оказалось «**93% ответов содержат хотя бы один действительно покрывающий тест**» при медианной точности 1.00. Ответ получают 57% покрытых символов (deep; 43% fast), **остальные 16% — `unknown`, никогда не «не покрыто»**; доминирующая причина названа (метод на сконструированном объекте). **D4 (шов фикстур) замерен и намеренно не построен:** на сьюте **с** conftest только **1 символ из 1043** достижим лишь через фикстуру. Тесты 484→**503**. Дизайн `../docs/design/test_mapping.md` (D1–D6 закрыты), доки `../docs/test-mapping.md`. |
| **[graph_provenance_2026-08-25.md](graph_provenance_2026-08-25.md)** | Провенанс артефакта, из **прожитого эпизода** R1-C22-f1: тест детерминизма покраснел, а причиной был **сдвинувшийся вход**, и отличить это от недетерминизма было нечем. У `graph.json` четыре ключа — ни версии тула, ни ревизии исходника, ни тира, ни `scope_id`; `codemap_schema` **пишется и никогда не читается**. **Замер:** одна замороженная копия, два codemap'а в 4 коммитах — **30 рёбер против 38**, `dead-code high` **12 против 7**, **оба объявляют `0.11`**, тул грузит любой молча. Причём схему бампать было не за что — менялись только открытые `extras`: **провенанс это не схема**. `diff` на этой паре отвечает «✅ No breaking changes» — верно и потому бесполезно. Сайдкар не спасает: отдельный файл, в `.gitignore`, best-effort (у живого bquant-сайдкара **нет `scope`**) и пишет `cwd`. **Закрыт в тот же день** вехой **R1-C25** (2026-08-25, схема **0.11→0.12**): блок `provenance` в графе (tool/tier/scope_id/source, **без часов и без абсолютных путей**), `codemap_schema` наконец **читается** (предупреждение через общий канал diagnostics → CLI, `stats`, три отчёта), `diff` сверяет сравнимость **до** вердикта. Граф bquant байт-идентичен старому за вычетом нового блока; две CLI-сборки байт-идентичны включая `provenance`; тесты 425→**453**. **Чего дизайн не предвидел:** у `--incremental` та же слепота — решал по одному дереву исходников и после апгрейда возвращал вчерашний граф; теперь сверяет сборщика. Дизайн `../docs/design/graph_provenance.md` (D1–D7 закрыты), доки `../docs/provenance.md`. **Разблокировало** R1-C23 (D2/D6) и пост блога P4. |
| **[deep_tier_regression_2026-08-25.md](deep_tier_regression_2026-08-25.md)** | Из issue [#10](https://github.com/kogriv/codemap/issues/10), после проверки R1-C21 автором на своём таргете: импорт-граф ожил (0→407), но *«0 из 338 in-core вызовов пересекают границу модуля»*. **Регрессия между тирами:** `--deep` отдаёт **меньше**, чем бесплатный fast. Замерено на его же цели (37 модулей, плоский layout): fast **487 вызовов / 158 кросс-модульных**, deep **336 / 0**. Механизм — не «jedi не смог»: jedi резолвит `from leaf import helper` **правильно**, в `leaf.helper`, а тест `startswith(pkg + ".")` читает это как external. Тот же дефект, что R1-C21, этажом ниже: границу griffe научили, границу jedi — нет. **И это не только про плоский layout:** тиры были взаимоисключающими (`_resolve_jedi` **вместо** `_resolve`), поэтому deep терял настоящие рёбра и на упакованных целях — 5 на codemap, 5 на bquant. **Закрыт в тот же день** вехой **R1-C26**: `_flat_qualify` на границе jedi + **union вместо замены** (фолбэк при `unresolved` **и** при ответе, который не является узлом графа; на `external` — намеренно нет). `calls(deep) ⊇ calls(fast)`: fast-only **158→0** (кросс-модульные **0→234**), **5→0**, **5→1** — оставшаяся разница это deep **точнее**, а не потеря. Точность бенча **100%** без изменений. Дизайн `../docs/design/deep_tier_union.md` (D1–D5 закрыты), доки `../docs/flat-layout.md` §The deep tier. |
| **[unverified_claims_2026-08-27.md](unverified_claims_2026-08-27.md)** | Честность, **развёрнутая внутрь**: все прошлые гэпы — про уверенные утверждения тула о **мишени**, этот — про утверждения проекта **о себе**. Не обкатка и не репорт извне, а **замер готовности к выпуску**. **`requires-python >=3.10` оказалось ложью:** 3.10 — **13 failed**, 3.11 — **4 failed**, 3.12/3.13/3.14 — **512 passed**. Разложение чистое: 9 падений — нет `tomllib` (стдлиб с 3.11); 4 — фикстура на синтаксисе 3.12, и **два из них это R1-C23, работающий правильно** (файл назван с `reason: "syntax"`, поднят `unread_inputs`, «чистое дерево» честно не чистое). Под девяткой — дефект **на всех версиях** (→ R1-C27): `TOMLDecodeError` наследует `ValueError` и ловится тем же `except`, поэтому **одна убранная скобка** в `codemap.toml` меняет `exit 2` («14 импортов вверх по слоям») на «_nothing to enforce_», `exit 0` — **опечатка красит CI-гейт в зелёный**. Плюс: **отгружаемый артефакт никогда не ставили** (колесо собирается, ставится, работает вне дерева исходников — и `provenance.tool` из колеса корректно без `commit`/`dirty`, ветка R1-C25, впервые исполненная); метаданные пусты (машиночитаемо колесо **не заявляет лицензии вообще**); `readtags` и `scip` скипались **в каждом прогоне за всю историю проекта**. **Честная граница фикса:** CI **не поймал бы** самые поучительные провалы проекта (сдвинувшийся вход R1-C22-f1, два соврамших «до/после», обрезанный `gaps/README.md`) — их ловил человек; CI держит другой класс. **Закрыт в тот же день:** **R1-C27** (три условия разведены, `codemap/tomlio.py`, `check` даёт exit 2 на нечитаемом контракте) + веха **M20** (`>=3.11`, метаданные, GitHub Actions из 4 джоб, 0.0.3). Дизайн `../docs/design/release_engineering.md` (D1–D7 закрыты), доки `../docs/ci.md`. |
| **[limit_truncation_2026-08-28.md](limit_truncation_2026-08-28.md)** | **Найден в чужом туле, подтверждён в своём.** Первый замер T2 против CodeGraph (разбор R2) вернул ровно **20** вызывающих, все `kind:"file"` при `startLine:1` — читается как чистая находка «у него file-import-модель callers», годная в карточку, в матрицу и в пост. Ложь: `--limit` по умолчанию **20**, file-строки сортируются первыми, и дефолт обрезал ответ **ровно по линии, которая искажает модель** — на `--limit 500` там 79 записей, из них 58 символьных, и наши 57 оказываются их подмножеством. Флага `truncated` в payload нет. Тот же вопрос, заданный codemap: `search "zone"` при дефолтном `limit=50` отдаёт **50 из 1259**, конверт — `{"ok": true}`, ни тотала, ни маркера, ни эха лимита. Причём это **op открытия** («discovery entry point» для холодного агента, F9) — единственная, чья работа сказать, что вообще есть, отвечает 4% и выглядит полной. Класс тот же, что `risk:"none"` и #3, на ступень слабее: не уверенное **ничто**, а уверенное **частичное**. `_PARTIAL_OPS` это не ловит — там партиальность **резолва**, а лимит — второй, независимый источник нижней границы (`callers` помечен и лимита не имеет; `search` лимит имеет и не помечен ничем). **Решение (§4):** блок `limit {applied, returned, total, truncated}` в конверте **всегда**, когда op принимает лимит — включая `truncated:false`, потому что отсутствие поля само по себе неоднозначно; `total:null`, если счёт не по карману, но не пропуск. Ортогонально `epistemic`; схему не трогает. Отвергнуто: поднять дефолты — это двигает обрыв, а не убирает. **Открыт**, кандидат **R1-C28**. |
| **[path_origin_2026-08-29.md](path_origin_2026-08-29.md)** | **У графа не было одного начала координат для путей.** Заведено второй мишенью ([issue #12](https://github.com/kogriv/codemap/issues/12)) по п. 5 списка `bquearch#5`: `codemap tests` печатает готовую строку `pytest …`, и при корнях не в корне репозитория она называет путь, которого нет, — при этом выглядит ровно как тот, который есть. **Напечатанный путь оказался симптомом:** каждый корень был своим началом координат (ядро — относительно родителя пакета, потребитель — относительно своего), так что при `src/pkg` рядом с `tests/` в одном графе жили **две системы координат** и нигде об этом не говорилось. Тот же класс, что R1-C29 и R1-C30-f2: ответ, который читатель достраивает шире, чем он есть, — только достраивает не свойство, а систему координат. Плюс вторая половина: у символов под корнем-потребителем **не было `file` вообще** (1093 узла против 61), и `search` отвечал номером строки без файла. **Решение (§4):** одно начало координат (ближайший общий предок родителей корней) и `provenance.roots` относительно него вместо базовых имён; **где** это начало — машинная локация, уходит в сайдкар как `roots_base` (D5/D6: идентичность едет, рецепт остаётся дома); локальная команда разрешает id против сайдкара и печатает путь **только если файл действительно там**. **Закрыт: R1-C31**, схема 0.13. |
| **[import_map_module_level_2026-08-28.md](import_map_module_level_2026-08-28.md)** | **Слепое пятно, антикоррелирующее с вопросом.** Заведено с второй реальной мишени ([issue #11](https://github.com/kogriv/codemap/issues/11)) через несколько часов после того, как R1-C28 закрепил правило «всегда говори, чего не увидел». Импорт-карта строится **только по модульному уровню** — это уже записано в `hard_python_robustness_2026-08-25.md` как честное упущение *экстрактора*. Не записано, что оно делает с *потребителями*: `report architecture` при пустом списке циклов печатает `_none — import graph is acyclic._`, то есть **утверждение о свойстве**. У репортёра 47 модулей, **29 пар связаны только функционально-локальным импортом (26% графа)**, отчёт говорит 0 циклов, AST по всем импортам находит 2 — и **оба закрыты локальным импортом**, потому что локальный импорт — это ровно то, чем разработчик разрывает цикл. Воспроизведено на трёх файлах, включая асимметрию: **в одном и том же операторе вызов резолвится, а импорт — нет** (значит, дело не в поддержке, а в том, что карту строят только модульного уровня). **Это и наша мишень:** на `bquant` 35 из 277 внутрипакетных импортов (13%) — локальные, codemap рапортует **1 цикл, а их 41** — то есть публиковавшееся «codemap находит тот самый цикл» было нижней границей с recall 2.4%. Пересчёт вчерашнего сравнения с CodeGraph по общей истине: у нас precision 100% / recall 2.4%, у них 10% / 32% — они переоценивают и молчат, мы недооцениваем и **называем это свойством**; их механизм (обход рёбер вызовов) как раз и дал им recall, потому что call-тир локальные импорты видит. **Решение (§5):** (1) считать и всегда объявлять `import_map {module_level, function_local_skipped}` — тем же правилом, что и `limit`; (2) `_none — import graph is acyclic._` → `_no cycles found in the resolved import graph — M function-local import(s) were not resolved._` (девятое применение «unknown ≠ none» и **первое, где уверенный ответ — это свойство безопасности**); (3) потом резолвить их по-настоящему, помечая ленивость. **Закрыт: R1-C29** (карта импортов — 41 цикл из 41, множественно совпало с независимым эталоном; подтверждено репортёром на его дереве, §8) **и R1-C30** (§10 — та же слепота в *call*-слое: быстрый тир не резолвил вызов через локальный импорт, deep резолвил; +84 ребра на двух догфуд-мишенях, 84 из 84 подтверждены независимым deep-прогоном, карта пофункциональная, чтобы recall не был куплен ложными рёбрами). **Поправка по ходу:** первая версия §3 говорила «9 циклов» — ошибался наш собственный аудиторский скрипт, не тул; исправлено везде, где число публиковалось. |
| **[scope_membership_2026-09-02.md](scope_membership_2026-09-02.md)** | **Манифест описывает не тот набор файлов, из которого построен граф.** Заведено второй мишенью ([issue #15](https://github.com/kogriv/codemap/issues/15)) рядом с подтверждением починки R1-C36 — как пункт, который ею **не** закрывается: у них граф нёс 48 файлов, сайдкар перечислял 47 с `git_blob` и `sha256` на каждый, и артефакт, который читают, чтобы ответить «что именно проанализировано», отвечал неверно и без единого признака. **Замер показал дефект шире репорта:** та же дыра открывается **без подставного каталога** — неотслеживаемый (`git add` ещё не делали) или игнорируемый модуль есть в графе, отсутствует в `scope.files` и **не двигает `scope_id`**; два графа с разным содержимым пришли с одной идентичностью входа. `module_count_diagnostic` при этом молчит **правильно** — счёт экстрактора и граф сходятся, манифеста в том сравнении нет. **Причина:** два независимых определения слова «вход» — манифест перечисляет **отслеживаемый** набор (`git ls-files`), экстрактор обходит **файловую систему**; в fs-режиме они совпадают по построению (4 = 4), в git-режиме это разные множества, и ничто их не сравнивает. Дизайн ограничение знал и делегировал его `include`-оверлею — но в git-режиме `include` применяется **после** `ls-files`, он может убрать файл и никогда не может добавить, а у продуктового `resolve_scope` спеки нет вовсе; там же опровергнуто собственное утверждение дизайна «a dirty tree is still exact». **И это не отчётность:** `scope_id` — ключ кэша `--incremental` и проба `watch`; замер дал `[incremental] unchanged: 0 module(s) recomputed` над файлом, только что отрастившим новый символ. **Решение (§6):** D1 — перечислять то, что будет прочитано (`git ls-files --others --exclude-standard`, записи с `tracked: false`, цена 4 мс против 2); D2 — принадлежность как проверка, а не доверие (`file` каждого узла обязан быть в `scope.files`; игнорируемые **намеренно** остаются вне манифеста — про них честный ответ сказать, а не усыновить; только прямое направление, обратное шумит законно — 23 из 187, все объяснены); D3 — факт едет в `provenance.inputs`, потому что stderr сборки до потребителя не доезжает, **и путь при этом нельзя писать сырым**: ровно эти файлы чаще всего абсолютные, а блок provenance path-free по контракту и `build_provenance` на таком пути бросает. База ложных срабатываний замерена и равна нулю: на чистых сборках 0 из 51 и 0 из 164. **Закрыт в тот же день: R1-C41** (без схемы) — неотслеживаемый модуль теперь в манифесте с `tracked: false`, двигает `scope_id` и пересчитывается инкрементальной сборкой; игнорируемый назван, а не усыновлён; идентичность на чистом дереве не сдвинулась (`sha256:2c1da6e0…` старым и новым кодом), бенчмарк R2 пере-материализован в прежний id; мутационно 3 + 4 покраснения; сьют 734 → 743. **Ловушка, которой дизайн не предвидел:** у двух сторон **разное начало координат** — на раскладке `src/` наивное сравнение строк дало бы 2 ложных из 2 на здоровой сборке. **Поправка к собственному замеру:** «не бьёт ни разу» в первой версии §4 было неверно — померен был только класс `--ignored`, а репо-сборка мишени в тот же час несла **два неотслеживаемых** файла. |
| **[deep_tier_nondeterminism_2026-09-02.md](deep_tier_nondeterminism_2026-09-02.md)** | **Граф не воспроизводим от сборки к сборке — и это было известно, но сказано не там, где читают.** Заведено второй мишенью как «либо дефект, либо цена deep-резолвинга, и тогда назовите её в документации»: семь сборок неизменённого дерева, в одной пропало **настоящее** ребро через `getattr` (9524 против 9523). **Воспроизведено у себя на другом дереве:** десять deep-сборок → **два различных артефакта (7/3)**, дельта 0 рёбер и 2 узла из 2133 — один вызов переходит между `external` и `unresolved` при неизменных `out`/`resolved`. Локализовано до трёх вызовов `tgt.startswith`; порядок обхода модулей идентичен во всех прогонах, экстрактор однопоточный. **Отвергнуто по 10 прогонов на гипотезу:** хеш-сид (5/5 — гипотезу мишени, которую она сама назвала недопроверенной, закрыли с мощностью), дисковый кэш jedi/parso (5/5), сабпроцесс jedi (6/4), GC (7/3), ASLR (5/5), свежий `Project` на файл (3/7); стабилен только изолированный запрос по одному файлу — **100/100**. **Механизм:** пофункциональные счётчики исполнений jedi — исчерпавший бюджет вывод возвращает пустое множество, а пустое на нашей границе неотличимо от «нечего искать»; на первом расходящемся месте бюджет отличается **на единицу** при совпадающих 272 предыдущих классификациях. Почему на единицу — не установлено, это внутри jedi, и так и записано. **Вторая половина гэпа — наша:** предел измерен ещё в R1-C9 и жил в комментарии к `ci.yml`; `docs/ci.md` ссылался на `provenance.md`, где стояло обратное; README трижды говорил `deterministic` без тира; единственное место, где предел назывался (`incremental.md`), объясняло его **неверной** причиной («тёплый кэш» — опровергнуто холодным). Потребитель прочитал написанное и на два наблюдения объявил регрессию, которой не было. **Решение (§7):** раскрыть, а не притворяться, что починили — нота `note` на каждом deep-графе с измеренными числами, `caveats` в `comparability()` отдельно от `differences` (пара остаётся сравнимой, шумовой порог печатается над вердиктом, на смешанных тирах caveat не выдаётся), числа в четырёх документах и поправка причины в пятом. **Закрыт: R1-C42**, схема не менялась. |
| **[incremental_noise_persistence_2026-09-02.md](incremental_noise_persistence_2026-09-02.md)** | **`--incremental` шум deep-тира не наследует, а консервирует — и слепнет от него.** Заведено из собственного §9 предыдущего гэпа: пункт «чего не проверяли», продублированный потребителю как **названная, но не замеренная** гипотеза. Замерено на копии `bquant@cf7c6e1` (88 модулей, одиночный пакет): восемь полных deep-сборок неизменного дерева → **три** различных артефакта, весь разброс локализован в **одном методе**, зондовое `accesses`-ребро присутствует в **6 из 8**. **(1) Выборка замораживается:** от графа без ребра пять инкрементальных сборок подряд вернули его **0 раз**, полные сборки того же самого дерева — **5 из 5**; счётчики застывают побайтно (`deepcopy`). То есть стандартный совет «пересобери и посмотри, на месте ли ребро» — который мы сами и дали потребителю — под инкрементом **не работает**. Обратное плечо: 3 из 3 ребро остаётся, значит своего шума splice не привносит, он консервирует чужой. **(2) Промах защищает сам себя:** правило инвалидации (b) читает **старый** граф, поэтому потерянное ребро — это потерянная причина пересчитать. Причинность доказана начисто: `pipeline` **не импортирует** `zone_features` (правило (a) не при делах), одна и та же правка `zone_features` при старом графе **с** ребром инвалидирует писателя, **без** ребра — нет. **(3) Оговорка «пока модуль снова не изменится» неверна:** пересчитанный модуль — новая выборка, ребро вернулось лишь в **4 из 9**. **Замерено 03.09 и опровергнуто — нами же:** сигнал «частичный пересчёт систематически слабее» (`13/0` не достигнуто 9 раз из 9) не выжил. Парный прогон 24 пары на одинаковых состояниях дерева: `13/0` — **5 из 24 в обоих плечах**, знаковый критерий `p = 1.000`. Прежняя выборка набиралась правилом (b), то есть **самим измеряемым дефектом**: писатель попадал в пересчёт только пока ребро в старом графе было. Смена триггера на правило (a) конфаунд снимает. Вывод R1-C43 не ослаблен, а **сужен до адреса**: вред в том, что модуль не пересчитывается, а не в том, что пересчитывается хуже. **Очевидная починка измерена и отвергнута:** «не сплайсить модуль, признавший `unresolved > 0`» захватывает **69 из 88** модулей (78%) — выше `_FULL_FALLBACK_FRACTION`, вырождается в полную пересборку. Сузить нельзя структурно: множество `unresolved` **по определению не имеет целей**, его нечем проиндексировать по изменившемуся модулю. **Опровергнуты четыре собственных утверждения** (четвёртое — сигнал из этого же гэпа, см. выше)**:** планка R1-C9 «байт-идентично полной пересборке» (сказана без тира — на deep недостижима), `docs/incremental.md` «introduces no divergence of its own», docstring теста с причиной «cache-warmth» (опровергнута накануне — правка дошла до доков и не дошла до этой копии). **Сделано: R1-C43, только раскрытие** — `provenance.incremental` (пишется всегда, включая `false`), нота `incremental_deep_splice` на deep-графах, планка R1-C9 разведена на «содержимое совпадает» + «артефакт не выдаёт себя за полную сборку». Двери (2) периодическая полная и (3) ограничение тира — кандидатом в BACKLOG. |

## Связанные материалы

- `../DESIGN.md` — продуктовый дизайн и границы v1 (§7); решение по repo-scope (§10.12)
- `../BACKLOG.md` — вехи M0–M12 и отложенные возможности
- (историческое) реестр гэпов инкубационного монорепо `bquant` — запись G13 (вне этого репо)
